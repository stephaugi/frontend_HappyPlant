{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "src", // or "src" if your app code is inside a src directory
    "paths": {
      "@/app/*": ["app/*"]
    }
  }
}
